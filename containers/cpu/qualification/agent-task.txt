Using the approved neutral-singlet water geometry, plan exactly two independent PySCF CPU B3LYP/def2-SVP calculations that both consume the same initial geometry and neither consumes output from the other: a single point and a geometry optimization. Express only those two independent stages through ChemSmart project YAML and CLI operations. Do not execute calculations or author native PySCF code.
